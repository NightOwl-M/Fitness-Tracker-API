<persistence xmlns="https://jakarta.ee/xml/ns/persistence" version="3.1">
    <persistence-unit name="app-unit">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Registrér dine entiteter her (eller brug scanning i avancerede setups) -->
        <class>app.entities.User</class>
        <class>app.entities.Role</class>
        <!-- Tilføj flere: <class>app.entities.X</class> -->

        <properties>
            <!-- JDBC defaults (overstyres i runtime via overrides/ENV) -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:postgresql://localhost:5432/app"/>
            <property name="jakarta.persistence.jdbc.user" value="postgres"/>
            <property name="jakarta.persistence.jdbc.password" value="postgres"/>

            <!-- Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>  <!-- dev-default: update -->
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.show_sql" value="true"/>

            <!-- HikariCP -->
            <property name="hibernate.hikari.maximumPoolSize" value="10"/>
            <property name="hibernate.hikari.minimumIdle" value="2"/>
            <property name="hibernate.hikari.poolName" value="ApiTemplatePool"/>
        </properties>
    </persistence-unit>
</persistence>
